#!/bin/bash
NUM_FAILURES=0

../../check.template @srcdir@ src/groebner/markov -q -p arb -ghybrid $@
NUM_FAILURES=$(($NUM_FAILURES+$?))

../../check.template @srcdir@ src/groebner/markov -q -p arb -gproject-and-lift $@
NUM_FAILURES=$(($NUM_FAILURES+$?))

# The script returns the number of failures encountered.
exit $NUM_FAILURES
