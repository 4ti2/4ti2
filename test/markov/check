#!/bin/bash
NUM_FAILURES=0

../check.template src/groebner/markov -q -ghybrid $@
NUM_FAILURES=$(($NUM_FAILURES+$?))

../check.template src/groebner/markov -q -gproject-and-lift $@
NUM_FAILURES=$(($NUM_FAILURES+$?))

# The script returns the number of failures encountered.
exit $NUM_FAILURES
