#!/bin/bash
NUM_FAILURES=0

../check.template @srcdir@ src/groebner/qsolve -q -m -o maxcutoff $@
NUM_FAILURES=$(($NUM_FAILURES+$?))

../check.template @srcdir@ src/groebner/qsolve -q -s -o maxinter $@
NUM_FAILURES=$(($NUM_FAILURES+$?))

# The script returns the number of failures encountered.
exit $NUM_FAILURES
