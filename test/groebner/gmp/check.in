#!/bin/bash
NUM_FAILURES=0

../../check.template @srcdir@ src/groebner/groebner -q -p arb -afifo
NUM_FAILURES=$(($NUM_FAILURES+$?))

../../check.template @srcdir@ src/groebner/groebner -q -p arb -aweighted
NUM_FAILURES=$(($NUM_FAILURES+$?))

# The script returns the number of failures encountered.
exit $NUM_FAILURES
